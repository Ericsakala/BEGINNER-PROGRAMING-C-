#include <iostream>
#include <fstream>
#include <string>
#include <bits/stdc++.h>
using namespace std;

void Display(){
	const int SIZE=10;

string holder;
string token,info;

int i=0;
ifstream read("grade.txt");
while(read.eof()){
for(token:info){
	if(read+1){
		
	}
}
 getline(read,info);
cout<<info;
}
}

int main(){
	
Display();
	
}
